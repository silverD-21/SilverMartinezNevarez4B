---
title: "Tarea 1. Diagnostico y Reporte"
format: pdf
author: "SIlver David Martinez Nevarez"
---

```{python}
#Diagnostico_nombre.py
#Simulador de pedidos
#Conceptos basicos : variables, inputs, condicionales, funciones y bucles

#Elegir una tematica de tienda y escribir 3 productos
productos = ["Coca" , "Sprite" , "Manzanita"]
precios = [50, 70, 40]

#Funcion para calcular total
def calcular_total(cantidad, precios):
    total = 0
    for i in range (len(cantidad)):
        total += cantidad[i] * precios[i]
    return total


#Menu para usuario (Outpusts)
print("Menu de Bebidas, Bienenido")
nombre = (input("Ingresa tu nombre: "))

cantidad = []

for i in range (len(productos)):
    print(f"{i+1} {productos[i]} - ${precios[i]}")
    cantidad_agg = int(input(f"Cuantos {productos[i]} desea?: "))
    cantidad.append(cantidad_agg)
    
total = calcular_total(cantidad, precios)


print(f"hola {nombre}, compraste:")
for i in range(len(productos)):
    if cantidad[i] > 0:
        print(f"{cantidad[i]} {productos[i]} - ${cantidad[i] * precios[i]}")
print(f"Total a pagar: ${total}")



```
